name: Build Neato site

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/neato-publish.yml"
      - "content/posts/**"
      - "content/**"
      - "templates/**"
      - "neato.php"
      - "neato.yaml"
      - "package.json"
      - "package-lock.json"
      - "scripts/**"
      - "tina/**"
      - ".pages.yml"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: yaml

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install Node dependencies
        run: npm ci

      - name: Build site with Neato
        run: npm run neato:build

      - name: Build Tina admin
        shell: pwsh
        run: |
          if ([string]::IsNullOrWhiteSpace($env:TINA_TOKEN)) {
            Write-Host "Skipping Tina admin build because TinaCloud secrets are not configured yet."
            exit 0
          }
          npm run tina:build -- --skip-cloud-checks
          Remove-Item -LiteralPath published/admin/.gitignore -Force -ErrorAction SilentlyContinue
        env:
          TINA_TOKEN: ${{ secrets.TINA_TOKEN }}

      - name: Commit generated site
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Build Neato site"
          file_pattern: "published/**"
