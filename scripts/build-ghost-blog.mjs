import fs from 'node:fs/promises';
import path from 'node:path';
const ghostUrl=(process.env.GHOST_URL||'https://cms.jewit.ch').replace(/\/$/,'');const contentKey=process.env.GHOST_CONTENT_API_KEY;const publicBase=(process.env.BLOG_PUBLIC_BASE||'/blog-next').replace(/\/$/,'');const outputRoot=process.env.BLOG_OUTPUT||'published/blog-next';const siteUrl='https://jewit.ch';if(!contentKey)throw new Error('GHOST_CONTENT_API_KEY is required.');
const esc=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');const date=v=>new Intl.DateTimeFormat('en-US',{year:'numeric',month:'long',day:'numeric',timeZone:'UTC'}).format(new Date(v));const postUrl=p=>`${siteUrl}${publicBase}/${p.slug}/`;const tagUrl=t=>`${publicBase}/tag/${t.slug}/`;
async function ghost(endpoint,params={}){const u=new URL(`${ghostUrl}/ghost/api/content/${endpoint}/`);u.searchParams.set('key',contentKey);Object.entries(params).forEach(([k,v])=>u.searchParams.set(k,v));const r=await fetch(u);if(!r.ok)throw new Error(`Ghost API ${r.status}: ${await r.text()}`);return r.json()}async function write(rel,content){const f=path.join(outputRoot,rel);await fs.mkdir(path.dirname(f),{recursive:true});await fs.writeFile(f,content)}
function shell({title,description,canonical,body,type='website',image=''}){const im=image?`<meta property="og:image" content="${esc(image)}"><meta name="twitter:card" content="summary_large_image">`:'<meta name="twitter:card" content="summary">';return `<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#160d1d"><title>${esc(title)} | Jewitch</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${esc(canonical)}"><link rel="alternate" type="application/rss+xml" title="Jewitch Blog" href="${siteUrl}${publicBase}/rss.xml"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:type" content="${type}"><meta property="og:url" content="${esc(canonical)}">${im}<link rel="stylesheet" href="/assets/site.css"><link rel="stylesheet" href="${publicBase}/ghost-blog.css"></head><body class="ghost-blog"><div class="witch-sky" aria-hidden="true">✦　·　☾　✧　·　✦</div><div class="site"><header class="site-header witch-header"><div class="witch-mark" aria-hidden="true">☾</div><h1 class="site-title"><a href="/">Jewitch</a></h1><div class="tagline">Musings of a Jewish Witch</div><nav class="site-nav"><a href="/">Home</a><a href="${publicBase}/">Blog</a><a href="${publicBase}/archive/">Archive</a><a href="/about/">About</a><a href="/now/">Now</a></nav></header><div class="content ghost-content"><main>${body}</main></div><footer class="site-footer witch-footer"><span aria-hidden="true">✦ ☾ ✦</span><br>Copyright 2026 Shira · Published with Ghost</footer></div></body></html>`}
const tagLinks=p=>(p.tags||[]).map(t=>`<a class="ghost-tag" href="${tagUrl(t)}">${esc(t.name)}</a>`).join(' ');const card=p=>`<article class="ghost-card">${p.feature_image?`<a class="ghost-card-image" href="${publicBase}/${esc(p.slug)}/"><img src="${esc(p.feature_image)}" alt="${esc(p.feature_image_alt||'')}"></a>`:''}<div class="ghost-card-copy"><div class="ghost-kicker">✦ ${date(p.published_at)}${p.reading_time?` · ${p.reading_time} min read`:''}</div><h2><a href="${publicBase}/${esc(p.slug)}/">${esc(p.title)}</a></h2><p>${esc(p.custom_excerpt||p.excerpt||'')}</p><div>${tagLinks(p)}</div></div></article>`;
await fs.rm(outputRoot,{recursive:true,force:true});await fs.mkdir(outputRoot,{recursive:true});const {posts=[]}=await ghost('posts',{limit:'all',include:'tags,authors',order:'published_at desc'});const tagsMap=new Map();for(const p of posts)for(const t of(p.tags||[])){if(!tagsMap.has(t.slug))tagsMap.set(t.slug,{...t,posts:[]});tagsMap.get(t.slug).posts.push(p)}
await write('ghost-blog.css',`:root{--eggplant-0:#0d0812;--eggplant-1:#160d1d;--eggplant-2:#24122e;--eggplant-3:#351942;--plum:#6f3c78;--lavender:#d8b9e6;--moon:#f1dca7;--mist:#eadff0;--muted:#b9a7bf}.ghost-blog{background:radial-gradient(circle at 50% -20%,#452052 0,#1c1025 35%,#0d0812 85%) fixed!important;color:var(--mist)!important;min-height:100vh}.ghost-blog a{color:var(--lavender)}.ghost-blog .site{background:linear-gradient(180deg,rgba(36,18,46,.84),rgba(13,8,18,.94));border:1px solid rgba(216,185,230,.16);box-shadow:0 0 60px rgba(0,0,0,.55),inset 0 0 45px rgba(111,60,120,.08)}.witch-sky{text-align:center;color:var(--moon);letter-spacing:1.1rem;padding:1rem 0 .15rem;opacity:.65;font-size:.85rem}.witch-header{text-align:center;position:relative;border-bottom:1px solid rgba(216,185,230,.18)!important}.witch-mark{font-size:2.3rem;color:var(--moon);filter:drop-shadow(0 0 10px rgba(241,220,167,.3));line-height:1}.ghost-blog .site-title a{color:var(--mist)!important;text-shadow:0 0 22px rgba(216,185,230,.18)}.ghost-blog .tagline{color:var(--muted)}.ghost-blog .site-nav a{color:var(--lavender)!important}.ghost-content{max-width:980px}.ghost-hero{padding:3rem 0 2.2rem;margin-bottom:1.4rem;text-align:center;position:relative}.ghost-hero:after{content:'✦  ☾  ✦';display:block;color:var(--moon);font-size:.75rem;letter-spacing:.65rem;margin-top:2rem;opacity:.75}.ghost-hero h1{font-family:Georgia,'Times New Roman',serif;font-size:clamp(2.8rem,8vw,5.8rem);font-weight:400;line-height:.94;margin:.25rem 0 1rem;color:#f4eaf7;text-shadow:0 3px 30px rgba(111,60,120,.3)}.ghost-hero p{font-size:1.12rem;max-width:42rem;margin-inline:auto;color:var(--muted)}.ghost-kicker,.ghost-post-meta{font-size:.78rem;text-transform:uppercase;letter-spacing:.12em;color:var(--moon);opacity:.82}.ghost-grid{display:grid;gap:1rem}.ghost-card{display:grid;grid-template-columns:minmax(0,240px) 1fr;gap:1.35rem;padding:1.35rem;background:linear-gradient(135deg,rgba(53,25,66,.55),rgba(22,13,29,.5));border:1px solid rgba(216,185,230,.12);border-radius:16px;box-shadow:0 10px 35px rgba(0,0,0,.2);transition:transform .18s ease,border-color .18s ease}.ghost-card:hover{transform:translateY(-2px);border-color:rgba(241,220,167,.3)}.ghost-card-image img,.ghost-feature{width:100%;height:auto;display:block;border-radius:11px;border:1px solid rgba(216,185,230,.12)}.ghost-card h2{font-family:Georgia,'Times New Roman',serif;margin:.3rem 0;font-size:clamp(1.55rem,4vw,2.25rem);font-weight:400}.ghost-card h2 a{text-decoration:none;color:#f1e6f4}.ghost-card p{color:var(--muted)}.ghost-tag{display:inline-block;margin:.35rem .3rem 0 0;padding:.22rem .6rem;border:1px solid rgba(216,185,230,.35);border-radius:999px;font-size:.72rem;text-decoration:none;color:var(--lavender)!important}.ghost-post-header{margin:2.8rem auto 2rem;text-align:center;max-width:52rem}.ghost-post-header h1{font-family:Georgia,'Times New Roman',serif;font-weight:400;font-size:clamp(2.6rem,7vw,5rem);line-height:.98;margin:.7rem 0;color:#f4eaf7}.ghost-deck{font-size:1.18rem;max-width:46rem;margin:1rem auto;color:var(--muted)}.ghost-feature{max-height:620px;object-fit:contain;background:rgba(0,0,0,.18);margin:0 auto 2rem}.ghost-post-body{max-width:760px;margin:0 auto;font-family:Georgia,'Times New Roman',serif;font-size:1.09rem;line-height:1.82;color:#eadff0}.ghost-post-body a{text-decoration-color:rgba(216,185,230,.45)}.ghost-post-body img{max-width:100%;height:auto;border-radius:8px}.ghost-post-body blockquote{margin:1.8rem 0;padding:.3rem 0 .3rem 1.4rem;border-left:2px solid var(--moon);color:var(--lavender);font-style:italic}.ghost-post-body hr{border:0;text-align:center;margin:2.5rem 0}.ghost-post-body hr:after{content:'✦  ☾  ✦';color:var(--moon);letter-spacing:.5rem}.ghost-archive-list{list-style:none;padding:0}.ghost-archive-list li{display:grid;grid-template-columns:10rem 1fr;gap:1rem;padding:.85rem 1rem;border-bottom:1px solid rgba(216,185,230,.12)}.ghost-archive-list time{color:var(--muted)}.witch-footer{text-align:center;color:var(--muted)!important}.witch-footer span{color:var(--moon);letter-spacing:.4rem}@media(max-width:650px){.witch-sky{letter-spacing:.55rem}.ghost-card{grid-template-columns:1fr;padding:1rem}.ghost-card-image img{max-height:260px;object-fit:cover}.ghost-archive-list li{grid-template-columns:1fr;gap:.15rem}.ghost-post-body{font-size:1.04rem}.ghost-hero{padding-top:2rem}}`);
const hero=`<section class="ghost-hero"><div class="ghost-kicker">☾ JEWITCH / THE BLOG ☽</div><h1>Random brain droppings.</h1><p>Writing, life, technology, religion, games, and whatever else escaped containment.</p></section>`;await write('index.html',shell({title:'Blog',description:'Random brain droppings from Jewitch.',canonical:`${siteUrl}${publicBase}/`,body:`${hero}<section class="ghost-grid">${posts.map(card).join('')||'<p>No posts yet.</p>'}</section>`}));
for(const p of posts){const authors=(p.authors||[]).map(a=>esc(a.name)).join(', ');const body=`<article><header class="ghost-post-header"><div class="ghost-kicker">${tagLinks(p)}</div><h1>${esc(p.title)}</h1>${(p.custom_excerpt||p.excerpt)?`<p class="ghost-deck">${esc(p.custom_excerpt||p.excerpt)}</p>`:''}<div class="ghost-post-meta">✦ ${date(p.published_at)}${authors?` · ${authors}`:''}${p.reading_time?` · ${p.reading_time} min read`:''} ✦</div></header>${p.feature_image?`<img class="ghost-feature" src="${esc(p.feature_image)}" alt="${esc(p.feature_image_alt||p.title)}">`:''}<div class="ghost-post-body">${p.html||''}</div></article><p style="text-align:center;margin-top:3rem"><a href="${publicBase}/">☾ Back to the blog</a></p>`;await write(`${p.slug}/index.html`,shell({title:p.meta_title||p.title,description:p.meta_description||p.custom_excerpt||p.excerpt||'',canonical:postUrl(p),type:'article',image:p.og_image||p.feature_image||'',body}))}
const archive=`<section class="ghost-hero"><div class="ghost-kicker">✦ THE GRIMOIRE ✦</div><h1>Archive</h1><p>${posts.length} published post${posts.length===1?'':'s'} preserved here.</p></section><ul class="ghost-archive-list">${posts.map(p=>`<li><time>${date(p.published_at)}</time><a href="${publicBase}/${esc(p.slug)}/">${esc(p.title)}</a></li>`).join('')}</ul>`;await write('archive/index.html',shell({title:'Archive',description:'The Jewitch blog archive.',canonical:`${siteUrl}${publicBase}/archive/`,body:archive}));for(const t of tagsMap.values()){await write(`tag/${t.slug}/index.html`,shell({title:`${t.name} — Blog`,description:t.description||`Posts tagged ${t.name}.`,canonical:`${siteUrl}${tagUrl(t)}`,body:`<section class="ghost-hero"><div class="ghost-kicker">✦ TAG ✦</div><h1>${esc(t.name)}</h1>${t.description?`<p>${esc(t.description)}</p>`:''}</section><section class="ghost-grid">${t.posts.map(card).join('')}</section>`}))}
const rss=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Jewitch</title><link>${siteUrl}${publicBase}/</link><description>Random brain droppings from Jewitch.</description>${posts.slice(0,50).map(p=>`<item><title>${esc(p.title)}</title><link>${postUrl(p)}</link><guid>${postUrl(p)}</guid><pubDate>${new Date(p.published_at).toUTCString()}</pubDate><description>${esc(p.custom_excerpt||p.excerpt||'')}</description></item>`).join('')}</channel></rss>`;await write('rss.xml',rss);const sitemap=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>${siteUrl}${publicBase}/</loc></url><url><loc>${siteUrl}${publicBase}/archive/</loc></url>${posts.map(p=>`<url><loc>${postUrl(p)}</loc><lastmod>${new Date(p.updated_at).toISOString()}</lastmod></url>`).join('')}${[...tagsMap.values()].map(t=>`<url><loc>${siteUrl}${tagUrl(t)}</loc></url>`).join('')}</urlset>`;await write('sitemap.xml',sitemap);console.log(`Built ${posts.length} post(s), ${tagsMap.size} tag page(s), archive, RSS, and sitemap.`);
